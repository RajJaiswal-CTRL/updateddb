🤖 Auto-Detection (Smart Parsing)

Automatically detects header row and data start row
Works with formatted Excel sheets with titles, empty rows, merged cells
Analyzes first 10 rows to identify structure
Shows confidence level: "Header at row X, Data from row Y"

📊 Dashboard Builder (Build Your Own Views)

Click "Build Dashboard" button
Select which charts you want (from categorical columns)
Select which statistics you want (from numeric columns)
Visual widget selector with click-to-toggle
Creates custom dashboard layout automatically

📋 Custom Table Builder (Create Filtered Tables)

Click "Create Table" button
Select columns to include (checkbox list)
Add filters: equals, contains, starts with, greater than, etc.
Sort by any column (ascending/descending)
Name your table and save it
Each table has its own search box and export

🎨 Features:

Smart Format Detection

Skips title rows automatically
Finds headers by analyzing text vs numbers ratio
Removes empty rows
Cleans Excel formatting artifacts


Multiple Custom Tables

Create unlimited filtered views
Each with different columns/filters
Independent search and export
Delete tables individually


Dynamic Charts

Choose which columns to visualize
Auto-selects bar vs donut based on data
Responsive and interactive


Powerful Filters

Equals, Contains, Starts with, Ends with
Greater than, Less than (for numbers)
Combine multiple filters


Export Everything

Export full dataset
Export individual custom tables
CSV format with proper escaping



🚀 Usage Flow:

Upload → System auto-detects structure
Build Dashboard → Select charts & stats
Create Tables → Filter, sort, customize
Export → Download any view as CSV
